こんにちは、<%= @resource.email %> 様

パスワード変更のリクエストを受け付けました。下記のリンクからパスワードの変更を行ってください。

<%= edit_password_url(@resource, reset_password_token: @token) %>

もし、このリクエストに心当たりがない場合は、このメールを無視してください。
上記のリンクにアクセスして新しいパスワードを設定するまで、パスワードは変更されません。
